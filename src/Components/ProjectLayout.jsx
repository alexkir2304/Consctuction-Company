import React from 'react';
import {Outlet} from "react-router";

const ProjectLayout = () => {
    return (
        <div>
            ProjectLayout
            <Outlet/>
        </div>
    );
};

export default ProjectLayout;